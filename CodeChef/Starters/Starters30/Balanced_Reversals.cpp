#include <bits/stdc++.h>
#define ll long long int
using namespace std;

int main()
{
    cin.sync_with_stdio(false);
    cin.tie(NULL);

    ll t;
    cin >> t; 
    while(t--){
        int n;
        string a;
        cin >> n >> a;
        int d[n];
        sort(a.begin(), a.end());
        cout << a << endl;
    }
    return 0;
}